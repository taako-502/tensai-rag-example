# RAG

RAGはRetrieval-Augmented Generationの略です。質問に関連する文書を検索し、その検索結果をプロンプトへ追加してから言語モデルに回答させます。

このサンプルでは文書をTF-IDFベクトルに変換し、コサイン類似度が高いチャンクを検索します。ベクトル同士の内積にはtensai.DotVecを使用します。

TF-IDFは単語や文字の一致を使う疎ベクトル検索です。意味埋め込みではないため、表現が大きく異なる質問の検索精度には限界があります。
